import React, { FC } from 'react';

import { chatMainDefaultStyle } from './style';

const TelbbyServiceClient: FC = () => {
  // @TODO 여기에서 Service Client를 구현해나갑니다.
  return <div css={chatMainDefaultStyle} />;
};

export default TelbbyServiceClient;
